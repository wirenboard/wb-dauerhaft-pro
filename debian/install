# Python package is installed by pybuild (see setup.py).
# Non-Python files go here:

bin/wb-dauerhaft-pro /usr/bin

configs/wb-dauerhaft-pro.schema.json /usr/share/wb-mqtt-confed/schemas

# Default config — wb-mqtt-confed needs the file to exist to open the editor
configs/wb-dauerhaft-pro.conf /etc/

# Keep user edits across firmware updates and factory resets (wb-configs)
10wb-dauerhaft-pro /etc/wb-configs.d
