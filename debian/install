# Python package is installed by pybuild (see setup.py).
# Non-Python files go here:

configs/wb-dauerhaft-pro.schema.json /usr/share/wb-mqtt-confed/schemas

# Default (empty) config — wb-mqtt-confed needs the file to exist to open the editor
configs/wb-dauerhaft-pro.conf /etc/
